[project]
name = "webpage_summarizer"
version = "0.1.0"
requires-python = ">=3.10"
description = "Fetch a webpage by URL, extract readable text with BeautifulSoup, return structured summary with title, main content excerpt, and key points."
dependencies = ["requests", "beautifulsoup4"]
